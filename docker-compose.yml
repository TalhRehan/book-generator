version: "3.8"

services:
  api:
    build: .
    container_name: book_generator_api
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./input:/app/input
      - ./output:/app/output
    restart: unless-stopped